{
  "name": "rv-automoviles",
  "version": "1.0.0",
  "description": "Sistema de gestión digital para automotora - RV Automoviles",
  "private": true,
  "scripts": {
    "install-all": "cd api && npm install && npm run prisma:generate && cd ../frontend && npm install",
    "install:vercel": "cd api && npm install && npm run prisma:generate && cd ../frontend && npm install",
    "build:vercel": "cd api && npm run prisma:migrate:deploy && cd ../frontend && npm run vercel-build",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd api && node index.js",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "setup": "node setup-database.js",
    "db:setup": "cd api && npx prisma generate --schema=./prisma/schema.prisma && npx prisma db push --schema=./prisma/schema.prisma",
    "db:seed": "cd api && node prisma/seed.js",
    "db:check": "node setup-database.js"
  },
  "keywords": ["automanager", "car-dealership", "management-system", "rv-automoviles"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
